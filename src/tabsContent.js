import React from "react"

export default class TabsContent extends React.Component {
    render() {
        return (
            <div>
                {this.props.content}
            </div>
        )
    }
}
